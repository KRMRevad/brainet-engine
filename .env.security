# ==========================================
# Security Configuration for Testing
# ==========================================

# JWT Secret (MUST be set in production)
JWT_SECRET=dev-secret-CHANGE-IN-PRODUCTION-e8f4c9d2a1b5e7f3

# Admin Password for /api/auth/login
ADMIN_PASSWORD=test-admin-pass-CHANGE-IN-PRODUCTION

# CORS Origins (whitelist only trusted origins)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# LLM Provider (use 'ollama' for local)
LLM_PROVIDER=ollama
OLLAMA_URL=http://localhost:11434

# Workspace paths
WORKSPACE_PATH=/Volumes/Seagate_500/EVAD/1_BRAINET_MVP

# Rate limiting
API_RATE_LIMIT=60
